---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Sidebar from '../../components/Sidebar.astro';
import PostCard from '../../components/PostCard.astro';
import { getPublishedPosts, getAllTags } from '../../utils/collections';

const suppliers = await getCollection('supplier', ({ data }) => !data.draft);
suppliers.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const recentPosts = await getPublishedPosts();
const allTags = await getAllTags();
---

<Base title="Battle Lines Drawn Suppliers">
  <div class="wrap">
    <div class="post_content">
      <div class="page-header"><h1>Suppliers</h1></div>
      <p>All of the wargaming suppliers used by the Battle Lines Drawn blog. I am not sponsored by any of these suppliers, I pay for things exactly the same as you do. If I ever receive sponsored material (I won't), that will be noted prominently.</p>
      <ul class="post-list">
        {suppliers.map(s => (
          <PostCard
            title={s.data.title}
            href={`/supplier/${s.slug}/`}
            date={s.data.date}
            description={s.data.description}
            thumbnail={s.data.thumbnail}
            tags={s.data.tags}
          />
        ))}
      </ul>
    </div>
    <Sidebar recentPosts={recentPosts} allTags={allTags} />
  </div>
</Base>
